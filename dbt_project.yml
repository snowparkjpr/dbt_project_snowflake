name: dbt_project_snowflake
version: 1.0.0
config-version: 2
profile: dbt_project_snowflake
model-paths:
  - models
analysis-paths:
  - analyses
test-paths:
  - tests
seed-paths:
  - seeds
macro-paths:
  - macros
snapshot-paths:
  - snapshots
models:
  dbt_project_snowflake:
    +materialized: view
